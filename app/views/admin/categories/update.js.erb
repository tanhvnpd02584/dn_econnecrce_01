<% if @category.errors.any? %>
  $(".errors").html("ERROR(S): <%= j @category.errors.full_messages.join('; ') %>")
<% else %>
  $('table#categories tbody').append("<%= j render @category %>");
  $("#notice").html("<%= flash.now[:notice] %>");
<% end %>
